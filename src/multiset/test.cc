#include "s21_multiset.h"
#include <set>
#include <iostream>

int main(){
     s21::multiset<int> multi;
    // auto it = multi.begin();
    // it++;
    // std::cout << *it;
    // s21::multiset<int> my_multiset;
    // my_multiset.insert(4);

    return 0;
}