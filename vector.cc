#include <vector>
#include <iostream>
using namespace std;
#define print cout


int main() {
    vector<int> obj_vector;
   // std::cout << obj_vector.max_size() << std::endl;

//    std::cout << obj_vector.empty() << std::endl;
  //  obj_vector.push_back(0);
    // obj_vector.pop_back();
    // std::cout << obj_vector.empty() << std::endl;
    // std::cout << obj_vector.capacity() << std::endl;

    // for (int i = 0; i < 100; i++)
    //     obj_vector.push_back(8);
        
    // std::cout << *obj_vector.begin();
    // //for (int i = 0; i < 100; i++)
    // std::cout << obj_vector.capacity() << std::endl;
    // std::cout << obj_vector.size() << std::endl;

    //std::cout << (obj_vector.front()) << std::endl;
    //std::cout << (obj_vector.back()) << std::endl;
    //std::cout << obj_vector.empty() << std::endl;
    /*
    std::cout << obj_vector[0] << " " << endl;
    std::cout << "size: " << obj_vector.size() << endl;
    obj_vector.pop_back();
    std::cout << "size: " << obj_vector.size() << endl;
    obj_vector.pop_back();
    std::cout << "size: " << obj_vector.size() << endl;
    std::cout << obj_vector[1];
    */

   // std::cout << obj_vector.at(1);
    //obj_vector.pop_back();
    // numbers.push_back(3);
    // numbers.push_back(5);
    // print << numbers[0];

    // print << numbers.at(0);
    // print << numbers.at(0) << endl;
    // //vector<int>::iterator it = numbers.begin();
    // for (vector<int>::iterator i = numbers.begin(); i != numbers.end(); i++)
    //     print << *i << " ";


    return 0;
    
}